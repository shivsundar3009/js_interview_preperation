In JavaScript, '==' is the equality operator, which checks if the values are equal after type coercion, 
  while '===' is the strict equality operator, which checks both value and type
  , without performing type coercion.
